JOIN 문제인데 JOIN 안쓰고 품
SELECT B.ANIMAL_ID AS ANIMAL_ID , B.NAME AS NAME
FROM ANIMAL_OUTS B
WHERE B.ANIMAL_ID NOT IN (SELECT ANIMAL_ID
                          FROM ANIMAL_INS )
ORDER BY B.ANIMAL_ID

JOIN 쓴 풀이
RIGHT OUTER JOIN이 좌우바꿔서 LEFT OUTER JOIN으로 써도됨
WHERE 조건절을 A.ANIMAL_ID나 INTAKE_CONDITION 같이 INS테이블에잇는 아무거나해도됨 왜냐 다 NULL 값이 있기 때문

SELECT B.ANIMAL_ID AS ANIMAL_ID ,  B.NAME AS NAME
FROM ANIMAL_INS A RIGHT OUTER JOIN ANIMAL_OUTS B
ON A.ANIMAL_ID = B.ANIMAL_ID
WHERE INTAKE_CONDITION IS NULL
ORDER BY ANIMAL_ID
